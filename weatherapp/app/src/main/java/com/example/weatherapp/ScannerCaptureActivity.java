package com.example.weatherapp;

import com.journeyapps.barcodescanner.CaptureActivity;

public class ScannerCaptureActivity extends CaptureActivity {
    // Pode ficar vazio, serve apenas para customizar o scanner
}
